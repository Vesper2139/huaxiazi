# 智能编排与质量保障

表达润色与提示词优化共用 `ProfessionalizationPlanner` 和 `ProfessionalQualityValidator`。系统先解析场景、用途、风险、事实锚点与不确定表述，再把内置或用户指定的表达策略作为低优先级方法加入执行计划。

生成结果在本地检查人物、数字、日期、否定关系和承诺强度。仅有格式、冗余或语气问题时最多修复一次；存在事实风险时初稿不会展示或归档，修复后仍有风险则明确停止。

本地偏好只由用户接受、编辑、重试和风格切换等显式信号形成结构化统计，不再从历史正文反推，也不保存“模型稿 → 用户稿”的样本文本。
