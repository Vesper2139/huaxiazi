# Agent 架构研究依据

本项目借鉴成熟研究中的“可验证边界”，不复制特定框架：

- ReAct：对应有限步数、工具输出清洗和可审计 trace，不暴露思维链。
- LoCoMo / Mem0：对应分层记忆、同意、过期、敏感级别和事实锚点门禁。
- 混合检索：词法精确匹配与可插拔稠密评分组合，知识以 `reference-only` 进入上下文。

研究指标不能直接替代产品验收。生产变更必须同时验证事实保真、安全通过率、约束稳定性、工具副作用和上下文预算；真实模型或用户授权数据缺失时，只能标记为策略层能力。
